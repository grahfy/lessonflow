# Implementation Plan: System Logs and Improved Error Handling

**Track ID:** system-logs_20260310
**Spec:** spec.md
**Created:** 2026-03-10
**Status:** [ ] Not Started

## Overview

This plan covers the addition of a database-backed logging system, an admin interface to view these logs, a mechanism to send them to the developer, and a general refinement of error messages across the application.

## Phase 1: Database & Observability Foundation

Establish the data layer for logs and update the logging utility to persist information.

### Tasks

- [ ] Task 1.1: Add `SystemLog` model to `prisma/schema.prisma` with fields: `id`, `level`, `event`, `message` (Text), `meta` (Json), `createdAt`.
- [ ] Task 1.2: Run Prisma migration to update the database schema.
- [ ] Task 1.3: Update `src/lib/observability.ts` to include a database write operation in the `log`, `logEvent`, and `logError` functions.
- [ ] Task 1.4: Implement a background job or a hook in the logging service to purge logs older than 30 days to prevent table bloat.

### Verification

- [ ] Verify `SystemLog` table exists in the database.
- [ ] Manually trigger a log and confirm it appears in the database.
- [ ] Run unit tests for `observability.ts` ensuring DB writes are handled correctly (or mocked).

## Phase 2: Backend API & Email Service

Create the endpoints required for the UI to interact with logs and report bugs.

### Tasks

- [ ] Task 2.1: Create `GET /api/admin/system-logs` to fetch logs with pagination and level filtering.
- [ ] Task 2.2: Create `POST /api/admin/system-logs/report` to send selected logs or recent error context to contact@grahfmusic.com.
- [ ] Task 2.3: Update `src/lib/email/service.ts` if necessary to support the new bug report email template.

### Verification

- [ ] Test `GET /api/admin/system-logs` via curl/Postman.
- [ ] Test `POST /api/admin/system-logs/report` and verify an email is "sent" (check `OutboundEmail` table).

## Phase 3: Frontend UI

Integrate the log viewer and bug report form into the admin console.

### Tasks

- [ ] Task 3.1: Add "System Logs" to `ADMIN_NAV_ITEMS` in `src/lib/admin/config.ts`.
- [ ] Task 3.2: Create the `src/app/admin/system-logs/page.tsx` and associated client components.
- [ ] Task 3.3: Implement color-coding for log levels (e.g., Red for error, Yellow for warn, Blue/Green for info).
- [ ] Task 3.4: Add the "Report Bug" modal/form to the System Logs page.

### Verification

- [ ] Verify "System Logs" appears in the admin header and navigates correctly.
- [ ] Visually confirm logs are readable and color-coded.
- [ ] Submit a bug report from the UI and confirm success feedback.

## Phase 4: Error Handling Refinement

Audit the codebase for generic error messages and replace them with more descriptive ones.

### Tasks

- [ ] Task 4.1: Audit `src/lib` and `src/app/api` for generic `throw new Error(...)` calls.
- [ ] Task 4.2: Standardize error response objects to include a `code` field where appropriate.
- [ ] Task 4.3: Update specific files identified during research (e.g., `src/lib/invoices/currency.ts`, `src/lib/admin-auth.ts`) with better error details.

### Verification

- [ ] Confirm no regression in existing error flows.
- [ ] Manually trigger refined errors and verify the improved messages/codes in the UI or logs.

## Final Verification

- [ ] All acceptance criteria met
- [ ] Tests passing
- [ ] Documentation updated (if applicable)
- [ ] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
